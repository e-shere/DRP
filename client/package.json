{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@emotion/styled": "^11.9.3",
    "@mui/icons-material": "^5.8.4",
    "@mui/material": "^5.8.4",
    "@mui/x-data-grid": "^5.12.1",
    "axios": "^0.27.2",
    "pusher-js": "^7.1.1-beta",
    "react": "^18.1.0",
    "react-color": "^2.19.3",
    "react-dom": "^18.1.0",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "start": "rm -rf ./src/common/ && mkdir ./src/common && cp -r ../common/*.ts ./src/common && cp -r ../common/*.tsx ./src/common && react-scripts start && rm -rf ./src/common/",
    "build": "rm -rf ./src/common/ && mkdir ./src/common && cp -r ../common/*.ts ./src/common && cp -r ../common/*.tsx ./src/common && react-scripts build && rm -rf ./src/common/",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:4001",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/react-color": "^3.0.6"
  }
}
